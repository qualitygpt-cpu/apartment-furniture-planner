import { usePlanStore } from '../state/usePlanStore';
export function FurnitureLibrary(){ const {plan,setPlan}=usePlanStore(); if(!plan) return null; const add=(name:string,category:string)=>{plan.furniture.push({id:`f-${Date.now()}`,name,category:category as any,position:{x:1000,y:1000},rotation:0,width:900,depth:500,height:900,material:'wood',color:'#c4b5fd',primitives:[{id:'p1',type:'box',localPosition:{x:0,y:0},size:{width:900,depth:500},height:900}],collisionPolicy:'solid',clearanceZones:[]}); setPlan({...plan});}; return <div><h3>Furniture</h3><button onClick={()=>add('Wardrobe','wardrobe')}>+ Wardrobe</button><button onClick={()=>add('Chair','chair')}>+ Chair</button></div>; }
