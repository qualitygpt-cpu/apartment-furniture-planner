const prompt = `Analyze uploaded apartment PDF/image and return ONLY valid apartment-plan.v1.json. Use visible dimensions first; infer only when required. Units: millimeters. Coordinate system: top-left origin, x right, y down. Rooms and fixed elements must be orthogonal polygons. Walls must be explicit segments with thickness. Attach doors/windows via wallId with positionFromStart. Include confidence scores and metadata warnings for assumptions. Do not invent furniture unless clearly visible; unclear elements -> unknown.`;
export function AiPromptPanel(){ return <div><h3>AI Recognition Prompt</h3><textarea readOnly value={prompt} style={{width:'100%', minHeight:160}}/></div>; }
